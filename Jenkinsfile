pipeline {
    agent any

    echo 'Building client'

    stages {

        stage('build client') {
            steps {
                echo 'Building client'
            }
        }

        stage('build server') {
            steps {
                echo 'Building server'
            }
        }
    }
}