pipeline {
    agent any

    stages {

        stage('build client') {
            steps {
                echo 'Building client'
            }
        }

        stage('build server') {
            steps {
                echo 'Building server'
            }
        }
    }
}