#!groovy
# Best of Jenkinsfile
# `Jenkinsfile` is a groovy script DSL for defining CI/CD workflows for Jenkins


def clientCompile() {
    stage 'Compile client'
    node {
        echo 'COOL!'
    }

}

clientCompile();